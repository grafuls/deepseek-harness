/**
 * @deepseek-ai/dsh-collab-bundle — the multi-user overlay as a profile bundle
 * layer. The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-collab-bundle
 */

export {}
